---
import SearchForm from '~/components/search-form.astro'
import SearchOptionsContent from '~/components/search-options-content.astro'
import { t } from '~/i18n'
import Layout from '~/layouts/Layout.astro'
import { getPreferredLanguage } from '~/utils/language'

const acceptLanguage = Astro.request.headers.get('accept-language')
const userLanguage = getPreferredLanguage(acceptLanguage)

const title = t(userLanguage, 'search:searchOptions')
const crumbsData = [{ title }]
---

<Layout title={title} breadcrumbs={crumbsData}>
  <main>
    <article>
      <SearchForm>
        <SearchOptionsContent />
      </SearchForm>
    </article>
  </main>
</Layout>
